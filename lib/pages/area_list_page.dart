import 'package:flutter/material.dart';

class AreaListPage extends StatelessWidget {
  const AreaListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Area List Page'));
  }
}
